// Keep talking number from the user until the user enters a number which is mutliple of 7.
#include<stdio.h>

int main(){
    printf("Enter a number:")
    scanf("%d", &n);
    printf("%d\n",n);
    
    if(n%7 !=0){ //multiple of 7
        break;
    }
    }while(!);
    printf("thank you\n")
return 0;
}

// If the user input any integer the program will give an output on that input and if the number enterted by the user is a multiple of 7 the program will stop working and it will show thank you but if the input is not a multiple of 7 than the loop will work continously.
